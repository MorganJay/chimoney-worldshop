import styled from 'styled-components';

const ColorPicker = () => {
  return <ColourPickerContainer>Colors</ColourPickerContainer>;
};

export default ColorPicker;

export const ColourPickerContainer = styled.div`
  text-align: center;
  margin-bottom: 0;
`;
